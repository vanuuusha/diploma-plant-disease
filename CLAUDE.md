# Инструкции для Claude — Дипломная работа

## Кто ты в этом проекте

Ты помогаешь студенту-магистру НИТУ МИСИС (направление 09.04.01 Информатика и вычислительная техника, кафедра ИКТ) с написанием дипломной работы. У студента сильный бэкграунд в области ИИ (глубокое обучение, компьютерное зрение) и программирования (Python, PyTorch, Ultralytics). Научный руководитель — к.т.н., доцент Стучилин В.В.

Тема дипломной работы связана с информационной системой диагностики заболеваний растений на основе методов глубокого обучения. Две предыдущие КНИР (НИР1 и НИР2) уже выполнены и являются фундаментом для диплома.

---

## Обязательный порядок чтения при старте нового диалога

В начале каждого нового диалога, прежде чем приступать к любой работе, **обязательно прочитай** файлы в следующем порядке:

### 1. Инструкции и стиль
- `CLAUDE.md` — этот файл (ты уже его читаешь)
- `СТИЛЬ_НАПИСАНИЯ.md` — детальное описание стиля написания текста (тон, лексика, структура предложений, шаблоны)
- `recomendations.md` — рекомендации руководителя по содержанию КНИР

### 2. Контекст предыдущих работ (оба НИРа)
- `готовые работы/НИР1.md` — НИР1: обзор методов глубокого обучения, датасеты, прототип каскадной системы (AlexNet → ConvNeXt → Faster R-CNN)
- `готовые работы/НИР2.md` — НИР2: сравнение детекторов (Faster R-CNN, YOLOv8, YOLOv10, DETR, RT-DETR), итог — YOLOv10

### 3. Структура диплома
- `диплом/diplom_structure.md` — согласованная структура глав дипломной работы. Читай перед написанием любой главы, чтобы понимать общий план и не выходить за рамки.

### 4. Артефакты решений — папка `диплом/artifacts/`
- Прочитай **все файлы** из папки `диплом/artifacts/` — это зафиксированные решения, договорённости, концепции. Артефакты имеют формат `ART_NNN_НАЗВАНИЕ.md` и содержат выжимки ключевых решений по дипломной работе (новаторство, архитектура, эксперименты и т.д.). Без них ты будешь предлагать то, что уже обсуждено и решено.

### 5. Текущая работа — папка `диплом/`
- Прочитай **все файлы-главы** из папки `диплом/` (кроме `artifacts/` и `diplom_structure.md`) — это и есть дипломная работа в процессе написания. Без этого ты не будешь знать, что уже написано.

---

## Правила создания файлов глав

Каждая глава дипломной работы оформляется отдельным .md файлом в папке `диплом/`:

### Основные главы (нумерованные)
```
диплом/chapter_1.md
диплом/chapter_2.md
диплом/chapter_3.md
...
```

### Специальные разделы (ненумерованные)
```
диплом/введение.md
диплом/заключение.md
диплом/реферат.md
диплом/термины.md
диплом/сокращения.md
диплом/список_источников.md
```

### Требования к файлам глав
- Каждый файл начинается с заголовка первого уровня (`# Название главы`)
- В начале файла — краткий навигатор по подразделам (как в НИР1.md и НИР2.md)
- Весь текст главы — в одном файле, не дробить на подфайлы
- При редактировании существующей главы — редактировать тот же файл, не создавать новый

---

## Правила написания текста

1. **Всегда** перечитай `СТИЛЬ_НАПИСАНИЯ.md` перед написанием текста — там подробные инструкции по стилю, тону, лексике, шаблонам фраз
2. Стиль — академический научно-технический, безличный, без разговорных элементов
3. Терминология — двуязычная (русский + английский в скобках при первом упоминании)
4. Опирайся на контекст из НИР1 и НИР2 — дипломная работа является их логическим продолжением
5. При описании экспериментов — конкретные числа, метрики, код (стиль НИР2)
6. При теоретических обзорах — развёрнутый анализ с преимуществами/недостатками (стиль НИР1)
7. **Никогда** не ссылаться на НИР1/НИР2 в тексте диплома (формулировки вида «что было подтверждено в НИР1», «как показано в рамках НИР2»). НИРы — это внутренний контекст для Claude, а не часть дипломной работы. Диплом — самостоятельный документ.

---

## Правила оформления

- Ссылки на рисунки: «на рисунке N» (до рисунка), подпись: «Рисунок N — Название»
- Ссылки на таблицы: «в таблице N», подпись: «Таблица N — Название»
- Кавычки — ёлочки « »
- При первом упоминании аббревиатуры — полная расшифровка + аббревиатура в скобках
- **Формулы — в LaTeX-синтаксисе:** инлайн `$formula$`, блочные `$$formula$$`. Пример: $F'_l = \gamma_l \odot F_l + \beta_l$
- **Формулы-уравнения всегда на отдельной строке** — если формула представляет собой самостоятельное уравнение или определение, она оформляется блочно (`$$...$$`) на отдельной строке, а не инлайн. Инлайн `$...$` используется только для упоминания переменных и коротких выражений внутри предложения.
- **Сноски-аннотации** — при упоминании нетривиального термина или явления (например, «проблема исчезающих градиентов», «mode collapse», «катастрофическое забывание») сразу после него в квадратных скобках даётся краткое пояснение (1–2 предложения), которое автор позже оформит как сноску в .docx. Формат: `[Краткое пояснение термина]`. Пример: «проблема исчезающих градиентов [При обратном распространении ошибки через большое число слоёв градиенты многократно умножаются на малые величины и экспоненциально затухают, что делает обновление весов ранних слоёв практически невозможным]».
- Подробные правила оформления: `оформление/recomendations.md` и `оформление/Чек-лист.md`

### Рисунки в тексте

Рисунки вставляются в текст в следующем формате:

```
Рисунок N — Подпись к рисунку. (Описание для вставки: краткое описание того, какую картинку нужно вставить + промпт для генерации в NanoBanano)
```

Правила:
- Рисунки нужно вставлять в местах, где это уместно — например, при описании архитектуры CNN, схемы детектора, пайплайна обработки и т.д.
- Перед рисунком в тексте обязательно ссылка: «на рисунке N показана...», «архитектура представлена на рисунке N»
- В скобках после подписи — описание-подсказка для автора: что именно изобразить и промпт для генерации через NanoBanano
- Рисунки из статей (архитектуры моделей, схемы, диаграммы) значительно повышают качество обзорной главы

### Насыщенность иллюстративным материалом

Текст дипломной работы должен быть хорошо разбавлен иллюстрациями, формулами, таблицами и диаграммами. Это важно как для наглядности, так и для объёма (количества страниц). Ориентиры:
- В обзорных главах — рисунок на каждую ключевую архитектуру/метод (схема архитектуры, блок-диаграмма, пайплайн)
- Формулы выносить в блочный формат при любой возможности — они визуально разбивают текст и добавляют научности
- Таблицы сравнения — везде, где сопоставляются 3+ объекта (модели, методы, датасеты)
- Диаграммы пайплайнов, блок-схемы алгоритмов — при описании любых процессов и архитектур

---

## Артефакты решений — `/save_art`

В процессе работы над дипломом ключевые решения фиксируются как артефакты в `диплом/artifacts/`. Для сохранения нового артефакта пользователь вызывает `/save_art`. Артефакт — это самодостаточная выжимка решения, которую любой новый чат сможет прочитать и понять без контекста предыдущего обсуждения.

---

## Код и эксперименты — папка `code/`

Весь код проекта (обучение, тестирование, инференс, утилиты) находится в папке `code/` в корне репозитория. Эта папка — самостоятельный проект со своей структурой.

### Структура `code/`
```
code/
├── README.md                  # Документация проекта (как запустить, зависимости, структура)
├── requirements.txt           # Зависимости Python
├── configs/                   # Конфиги экспериментов
├── data/                      # Скрипты подготовки данных, data.yaml
├── models/                    # Определения моделей, веса (в .gitignore)
├── scripts/                   # Скрипты обучения, тестирования, инференса
├── notebooks/                 # Jupyter-ноутбуки для анализа
├── results/                   # Результаты экспериментов (метрики, графики)
├── tasks/                     # Задачи для Claude Code на сервере (см. ниже)
│   ├── task_01/
│   │   ├── TASK.md            # Описание задачи (что сделать)
│   │   └── RESULT.md          # Результат выполнения (заполняет Claude Code)
│   ├── task_02/
│   │   ├── TASK.md
│   │   └── RESULT.md
│   └── ...
└── docs/                      # Техническая документация кода
```

### Система задач (tasks)

Проект использует два окружения:
- **Cowork (этот сеанс)** — написание текста диплома, анализ результатов, планирование
- **Claude Code на Linux-сервере** — выполнение тяжёлых задач (обучение моделей, эксперименты, тесты)

#### Как это работает:

1. **Cowork создаёт задачу** — файл `code/tasks/task_NN/TASK.md` с описанием того, что нужно сделать.
2. **Claude Code на сервере** читает `TASK.md`, выполняет задачу, пишет результат в `RESULT.md` и пушит на git.
3. **Пользователь пишет в Cowork:** «готово task_17» — значит задача выполнена.
4. **Cowork читает** `code/tasks/task_17/RESULT.md`, анализирует результаты и использует их в тексте диплома.

#### Формат TASK.md:
```markdown
# Task NN: Краткое название

## Статус
pending | in_progress | done

## Цель
Что нужно сделать и зачем.

## Шаги
1. ...
2. ...

## Входные данные
Где лежат данные, конфиги.

## Ожидаемый результат
Куда записать результат, какие метрики/артефакты сохранить.

## Результат записать в
`code/tasks/task_NN/RESULT.md`
```

#### Формат RESULT.md:
```markdown
# Result: Task NN

## Статус
done | failed | partial

## Что было сделано
Краткое описание выполненных действий.

## Результаты
Метрики, пути к артефактам, графикам.

## Проблемы / Замечания
Если что-то пошло не так.

## Артефакты
- `code/results/task_NN/...` — путь к сохранённым файлам
```

### Git

Весь проект ведётся в git-репозитории. Все изменения (код, результаты, текст) должны пушиться.
- Коммиты — с осмысленными сообщениями
- Тяжёлые файлы (веса моделей, датасеты) — в `.gitignore`
- Результаты экспериментов (метрики, графики, логи) — коммитятся
